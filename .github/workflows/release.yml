name: Package Releases

on:
  push:
    branches: [master]
    tags: ["*"]
  pull_request:
    branches: [master]

jobs:
  release:
    name: Create Release
    # if: startsWith(github.ref, 'refs/tags')
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
        with:
          submodules: recursive

      - name: Remove Unused Code
        run: |
          cp -r . /tmp/dist
          echo "INIT"
          du -sh /tmp/dist
          # Save ~396MB removing unused third_party.
          rm -rf /tmp/dist/alephzero/third_party/doctest /tmp/dist/alephzero/third_party/json /tmp/dist/alephzero/third_party/picobench
          echo "NO 3"
          du -sh /tmp/dist
          # We've removed doctest and picobench, so no point keeping C++ unit tests.
          rm -rf /tmp/dist/alephzero/src/bench /tmp/dist/alephzero/src/test
          echo "NO T"
          du -sh /tmp/dist
          # Save ~184MB dropping git history.
          rm -rf /tmp/dist/.git alephzero/.git alephzero/third_party/yyjson/.git
          echo "NO G"
          du -sh /tmp/dist
          # Save ~5MB dropping yyjson/test.
          rm -rf /tmp/dist/alephzero/third_party/yyjson/test
          echo "NO YT"
          du -sh /tmp/dist

      - name: Archive Source
        run: |
          tar -czvf ./alephzero-${GITHUB_REF##*/}.tar.gz /tmp/dist
          ls -l alephzero-${GITHUB_REF##*/}.tar.gz
          zip -r ./alephzero-${GITHUB_REF##*/}.zip /tmp/dist
          ls -l alephzero-${GITHUB_REF##*/}.zip

      # - name: Create Release
      #   id: create_release
      #   uses: actions/create-release@v1
      #   env:
      #     GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      #   with:
      #     tag_name: ${GITHUB_REF##*/}
      #     release_name: Release ${GITHUB_REF##*/}
      #     draft: false
      #     prerelease: false

      # - name: Upload Release Asset
      #   uses: actions/upload-release-asset@v1
      #   env:
      #     GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      #   with:
      #     upload_url: ${{ steps.create_release.outputs.upload_url }}
      #     asset_path: tar -czvf alephzero-${GITHUB_REF##*/}.tar.gz
      #     asset_name: tar -czvf alephzero-${GITHUB_REF##*/}.tar.gz
      #     asset_content_type: application/gzip

  pip:
    name: Send to PyPI
    if: startsWith(github.ref, 'refs/tags')

    runs-on: ubuntu-latest
    container:
      image: "quay.io/pypa/manylinux_2_24_x86_64"

    steps:
      - uses: actions/checkout@v2
        with:
          submodules: recursive

      - name: install apt deps
        run: apt update && apt install -y python3-pip

      - name: install pip deps
        run: python3 -m pip install -U build pybind11 setuptools twine virtualenv wheel

      - name: build package
        run: python3 -m build

      - name: auditwheel
        run: |
          for whl in dist/*-linux_x86_64.whl; do
            auditwheel repair "$whl" -w dist/
          done
          rm dist/*-linux_x86_64.whl

      - name: Publish distribution 📦 to Test PyPI
        uses: pypa/gh-action-pypi-publish@master
        with:
          password: ${{ secrets.TEST_PYPI_API_TOKEN }}
          repository_url: https://test.pypi.org/legacy/

      - name: Publish distribution 📦 to PyPI
        uses: pypa/gh-action-pypi-publish@master
        with:
          password: ${{ secrets.PYPI_API_TOKEN }}
